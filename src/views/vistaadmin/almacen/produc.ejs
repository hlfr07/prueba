<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" type="image/x-icon" href="/src/views/img/icono.ico">
    <title>PRODUCTOS</title>

    <!-- Custom fonts for this template-->
    <link href="/src/boostrapdiseñoadmin/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link
        href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i"
        rel="stylesheet">

    <!-- Custom styles for this template-->
    <link href="/src/boostrapdiseñoadmin/css/sb-admin-2.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/src/views/css/registrar.css">
    <link rel="stylesheet" href="/src/views/css/estiloperfiles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css"
        integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body id="page-top">

    <!-- Page Wrapper -->
    <div id="wrapper">

        <!-- Sidebar -->
        <%- include('../plantilla/listamodulos') %>
            <!-- End of Sidebar -->

            <!-- Content Wrapper -->
            <div id="content-wrapper" class="d-flex flex-column">

                <!-- Main Content -->
                <div id="content">

                    <!-- Topbar -->
                    <%- include('../plantilla/navbar') %>
                        <!-- End of Topbar -->

                        <!-- ACA DEBEMOS COPIAR LAS TABLAS O REGISTROS PARTE BLANCA -->
                        <div class="container-fluid overflow-auto">
                            <h1></h1>

                            <div class="row g-3">
                                <div class="col">
                                    <button type="button" class="btn btn-primary" data-bs-toggle="modal"
                                        data-bs-target="#exampleModal1">
                                        AGREGAR PRODUCTOS
                                    </button>
                                </div>
                                <div class="col">
                                    <input type="text" class="form-control" id="busqueda"
                                        placeholder="  🔍︎    Buscar Producto..."
                                        oninput="this.value = this.value.toUpperCase()">
                                </div>
                            </div>

                            <h1>PRODUCTOS EN EL SISTEMA</h1>
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID del Producto </th>
                                        <th>Codigo</th>
                                        <th>Nombre del Producto</th>
                                        <th>Categoria</th>
                                        <th>Unidad</th>
                                        <th>Descripcion</th>
                                        <th>Stock</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% producto.forEach(function(productos) { %>
                                        <tr>
                                            <td>
                                                <%= productos.idproducto %>
                                            </td>
                                            <td>
                                                <%= productos.codigo_producto %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_producto %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_categoria %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_unidad %>
                                            </td>
                                            <td>
                                                <%= productos.descripcion %>
                                            </td>
                                            <td>
                                                <%= productos.stock %>
                                            </td>
                                            <td>

                                                <button onclick="botoneditar(<%= productos.idproducto %>)" type="button"
                                                    class="btn btn-primary" data-bs-toggle="modal"
                                                    data-bs-target="#exampleModal">
                                                    <i class="bi bi-pencil-square"></i>
                                                </button>

                                                <button onclick="botoneliminar(<%= productos.idproducto %>) "
                                                    type="button" class="btn btn-danger"><i
                                                        class="bi bi-trash-fill"></i></button>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                            <h1>PRODUCTOS INACTIVOS</h1>
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID del Producto </th>
                                        <th>Codigo</th>
                                        <th>Nombre del Producto</th>
                                        <th>Categoria</th>
                                        <th>Unidad</th>
                                        <th>Descripcion</th>
                                        <th>Stock</th>
                                        <th>Acciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% productoInactivos.forEach(function(productos) { %>
                                        <tr>
                                            <td>
                                                <%= productos.idproducto %>
                                            </td>
                                            <td>
                                                <%= productos.codigo_producto %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_producto %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_categoria %>
                                            </td>
                                            <td>
                                                <%= productos.nombre_unidad %>
                                            </td>
                                            <td>
                                                <%= productos.descripcion %>
                                            </td>
                                            <td>
                                                <%= productos.stock %>
                                            </td>
                                            <td>
                                                <button onclick="botonveract(<%= productos.idproducto %>) "
                                                    type="button" class="btn btn-success">RESTABLECER</button>
                                            </td>
                                        </tr>
                                        <% }); %>
                                </tbody>
                            </table>
                            <!-- MODAL PARA AGREGAR CLIENTE-->
                            <div class="modal fade" id="exampleModal1" tabindex="-1" aria-labelledby="exampleModalLabel"
                                aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h1 class="modal-title fs-5" id="exampleModalLabel">AGREGAR PRODUCTOS</h1>

                                        </div>
                                        <div class="modal-body">
                                            <div class="right-container" style="padding: 20px; flex-grow: 1;">
                                                <form id="myForm">
                                                    <label for="codigo_producto">Codigo:</label>
                                                    <div class="input-group mb-3">
                                                        <input type="text" class="form-control"
                                                            placeholder="Ingrese el Codigo" id="codigo_producto"oninput="validarYConvertir2(this)">
                                                    </div>
                                                    <label for="nombre_producto">Nombre del Producto:</label>
                                                    <input type="text" placeholder="Ingrese el Nombre"
                                                        id="nombre_producto" oninput="validarYConvertir(this)">

                                                    <label for="nombre_categoria">Categoria:</label>
                                                    <select id="nombre_categoria">
                                                        <% categoria.forEach(function(cate) { %>
                                                            <option value="<%= cate.idcategoria %>">
                                                                <%= cate.nombre_categoria %>
                                                            </option>
                                                            <% }); %>
                                                    </select>

                                                    <label for="nombre_unidad">Unidad:</label>
                                                    <select id="nombre_unidad">
                                                        <% unidad.forEach(function(unid) { %>
                                                            <option value="<%= unid.idunidad %>">
                                                                <%= unid.nombre_unidad %>
                                                            </option>
                                                            <% }); %>
                                                    </select>

                                                    <label for="descripcion">Descripcion:</label>
                                                    <input type="text" placeholder="Ingresar Descripcion"
                                                        id="descripcion" oninput="validarYConvertir1(this)">

                                                    <label for="stock">Stock:</label>
                                                    <input type="text" placeholder="Stock del Producto" id="stock"
                                                        readonly>

                                                </form>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary"
                                                data-bs-dismiss="modal">Close</button>
                                            <button type="button" class="btn btn-primary"
                                                onclick="postDatos()">Agregar</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- MODAL PARA EDITAR-->
                            <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel"
                                aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h1 class="modal-title fs-5" id="exampleModalLabel">ACTUALIZAR PRODUCTO
                                            </h1>

                                        </div>
                                        <div class="modal-body">
                                            <div class="right-container" style="padding: 20px; flex-grow: 1;">
                                                <form id="myForm">
                                                    <label for="idproducto">ID:</label>
                                                    <input type="text" placeholder="Ingrese su ID" id="idproducto"
                                                        readonly>

                                                    <label for="codigo_producto1">Codigo Producto:</label>
                                                    <input type="text" placeholder="Ingrese su Codigo"
                                                        id="codigo_producto1" readonly>

                                                    <label for="nombre_producto1">Nombre del Producto:</label>
                                                    <input type="text" placeholder="Ingrese el Nombre"
                                                        id="nombre_producto1" oninput="validarYConvertir(this)">


                                                    <label for="nombre_categoria1">Categoria:</label>
                                                    <select id="nombre_categoria1">
                                                        <% categoria.forEach(function(cate) { %>
                                                            <option value="<%= cate.idcategoria %>">
                                                                <%= cate.nombre_categoria %>
                                                            </option>
                                                            <% }); %>
                                                    </select>

                                                    <label for="nombre_unidad1">Unidad:</label>
                                                    <select id="nombre_unidad1">
                                                        <% unidad.forEach(function(unid) { %>
                                                            <option value="<%= unid.idunidad %>">
                                                                <%= unid.nombre_unidad %>
                                                            </option>
                                                            <% }); %>
                                                    </select>

                                                    <label for="descripcion1">Descripcion</label>
                                                    <input type="text" placeholder="Ingrese direccion"
                                                        id="descripcion1" oninput="validarYConvertir1(this)">

                                                        <label for="stock1">Stock:</label>
                                                        <input type="text" placeholder="Stock del Producto" id="stock1"
                                                            readonly>

                                                </form>
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-secondary"
                                                data-bs-dismiss="modal">Close</button>
                                            <button type="button" class="btn btn-primary"
                                                onclick="guardarDatos()">Actualizar</button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        </div>
                        <footer class="sticky-footer bg-white">
                            <div class="container my-auto">
                                <div class="copyright text-center my-auto">
                                    <span>Copyright &copy; UNSM 2023</span>
                                </div>
                            </div>
                        </footer>
                </div>
                <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
                <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>


                <script>
                    function botoneditar(idproducto) {
                        // Asignar el ID del perfil al campo oculto

                        axios.get(`/admin/producto/${idproducto}`)
                            .then(function (response) {
                                const respuestadelquery = response.data;
                                if (respuestadelquery) {
                                    console.log(respuestadelquery); // Imprimir los datos en la consola
                                    document.getElementById('idproducto').value = respuestadelquery.idproducto;
                                    document.getElementById('codigo_producto1').value = respuestadelquery.codigo_producto;
                                    document.getElementById('nombre_producto1').value = respuestadelquery.nombre_producto;
                                    document.getElementById('nombre_categoria1').value = respuestadelquery.idcategoria;
                                    document.getElementById('nombre_unidad1').value = respuestadelquery.idunidad;
                                    document.getElementById('descripcion1').value = respuestadelquery.descripcion;
                                    document.getElementById('stock1').value = respuestadelquery.stock;

                                }
                            })
                            .catch(function (error) {
                                console.error(error);
                            });
                    }


                    function postDatos() {
                        // Obtener los valores de los campos de entrada
                        const codigo_producto = document.getElementById('codigo_producto').value;
                        const nombre_producto = document.getElementById('nombre_producto').value;
                        const idcategoria = document.getElementById('nombre_categoria').value;
                        const idunidad = document.getElementById('nombre_unidad').value;
                        const descripcion = document.getElementById('descripcion').value;
                        const stock = document.getElementById('stock').value;

                        // Crear un objeto de datos con los valores para usar en los controolers
                        const data = {
                            codigo_producto: codigo_producto,
                            nombre_producto: nombre_producto,
                            idcategoria: idcategoria,
                            idunidad: idunidad,
                            descripcion: descripcion,
                            stock: stock
                        };
                        console.log(data);

                        // Enviar una solicitud POST a la ruta "clientes" utilizando Axios
                        axios.post('/admin/producto', data)
                            .then(function (response) {
                                // Mostrar un mensaje de éxito con SweetAlert
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Registro exitoso',
                                    text: 'El Producto se realizó correctamente.',
                                    timer: 2000, // Cambia la duración del mensaje si lo deseas
                                    showConfirmButton: false // Oculta el botón "OK"
                                });

                                // Redirigir al usuario después de 2 segundos (2000 ms)
                                setTimeout(function () {
                                    window.location.href = '/admin/producto';
                                }, 2000);
                            })

                    }


                    function guardarDatos() {
                        // Obtén el valor del campo de entrada
                        // Obtener los valores de los campos de entrada
                        const idproducto = document.getElementById('idproducto').value;
                        const codigo_producto1 = document.getElementById('codigo_producto1').value;
                        const nombre_producto1 = document.getElementById('nombre_producto1').value;
                        const idcategoria1 = document.getElementById('nombre_categoria1').value;
                        const idunidad1 = document.getElementById('nombre_unidad1').value;
                        const descripcion1 = document.getElementById('descripcion1').value;
                        const stock1 = document.getElementById('stock1').value;

                        // Crear un objeto de datos con los valores para usar en los controolers
                        const data = {
                            idproducto: idproducto,
                            codigo_producto1: codigo_producto1,
                            nombre_producto1: nombre_producto1,
                            idcategoria1: idcategoria1,
                            idunidad1: idunidad1,
                            descripcion1: descripcion1,
                            stock1: stock1
                        };


                        axios.put(`/admin/producto/${idproducto}`, data)
                            .then(function (response) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Registro exitoso',
                                    text: 'El Producto se realizó correctamente.',
                                    timer: 2000,
                                    showConfirmButton: false
                                }).then(function () {
                                    // Después de que el usuario haya visto la notificación, actualiza la página
                                    location.reload(); // Esto recargará la página actual
                                });
                            })
                            .catch(function (error) {
                                console.error(error);
                            });

                    }


                    function botoneliminar(idproducto) {
                        // Utiliza SweetAlert2 para mostrar una ventana modal de confirmación
                        Swal.fire({
                            title: '¿Estás seguro?',
                            text: 'Esta acción eliminará el Producto con ID ' + idproducto,
                            icon: 'warning',
                            showCancelButton: true,
                            confirmButtonText: 'Sí, eliminar',
                            cancelButtonText: 'Cancelar'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                // El usuario confirmó la eliminación, realiza la solicitud DELETE con Axios
                                axios.delete(`/admin/producto/${idproducto}`)
                                    .then((response) => {
                                        // Si la solicitud DELETE se completa con éxito, puedes manejar la respuesta aquí
                                        console.log('Producto eliminado con éxito');
                                        // Puedes realizar acciones adicionales, como redirigir a otra página o actualizar la interfaz de usuario
                                        window.location.reload();
                                    })
                                    .catch((error) => {
                                        // Si hay un error en la solicitud DELETE, puedes manejarlo aquí
                                        console.error('Error al eliminar el Producto:', error);
                                        // Puedes mostrar un mensaje de error o realizar otras acciones según sea necesario
                                    });
                            }
                        });
                    }
                    function botonveract(idproducto) {
                        // Utiliza SweetAlert2 para mostrar una ventana modal de confirmación
                        Swal.fire({
                            title: '¿Estás seguro?',
                            text: 'Esta acción restablecera el Producto con ID ' + idproducto,
                            icon: 'warning',
                            showCancelButton: true,
                            confirmButtonText: 'Sí, restablecer',
                            cancelButtonText: 'Cancelar'
                        }).then((result) => {
                            if (result.isConfirmed) {
                                // El usuario confirmó la eliminación, realiza la solicitud DELETE con Axios
                                axios.delete(`/admin/borraproducto/${idproducto}`)
                                    .then((response) => {
                                        // Si la solicitud DELETE se completa con éxito, puedes manejar la respuesta aquí
                                        console.log('Producto restablecido con éxito');
                                        // Puedes realizar acciones adicionales, como redirigir a otra página o actualizar la interfaz de usuario
                                        window.location.reload();
                                    })
                                    .catch((error) => {
                                        // Si hay un error en la solicitud DELETE, puedes manejarlo aquí
                                        console.error('Error al restablecer Producto:', error);
                                        // Puedes mostrar un mensaje de error o realizar otras acciones según sea necesario
                                    });
                            }
                        });
                    }
                       //PARA HACER QUE SEA PURA MAYUSCULA Y SIN NUMEROS
          function validarYConvertir(input) {
            // Expresión regular que acepta solo letras y espacios
            var regex = /^[A-Za-z\s]+$/;

            if (!regex.test(input.value)) {
              // Si el valor no cumple con la expresión regular, elimina los caracteres no permitidos
              input.value = input.value.replace(/[^A-Za-z\s]+/, '');
            }

            // Convierte a mayúsculas
            input.value = input.value.toUpperCase();
          }
//MAYUSCULA Y NUMERO SIN ESPACIO
function validarYConvertir2(input) {
    // Expresión regular que acepta solo letras mayúsculas y números
    var regex = /^[A-Z0-9]+$/;

    // Convierte a mayúsculas y elimina caracteres no permitidos
    input.value = input.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
}

//MAYUSCULAS Y NUMERO CON ESPACIO
function validarYConvertir1(input) {
    // Expresión regular que acepta solo letras mayúsculas, números y espacios
    var regex = /^[A-Z0-9\s]+$/;

    // Convierte a mayúsculas y elimina caracteres no permitidos
    input.value = input.value.toUpperCase().replace(/[^A-Z0-9\s]/g, '');
}
                    //PARA BUSCAR EN MI TABLA
                    // Obtener referencia al campo de búsqueda
                    const campoBusqueda = document.getElementById('busqueda');

                    // Obtener todas las filas de la tabla
                    const filasTabla = document.querySelectorAll('tbody tr');

                    // Agregar un evento de entrada al campo de búsqueda
                    campoBusqueda.addEventListener('input', function () {
                        const consulta = campoBusqueda.value.toLowerCase();

                        // Iterar a través de las filas de la tabla y mostrar/ocultar según la consulta
                        filasTabla.forEach(function (fila) {
                            const contenidoFila = fila.textContent.toLowerCase();
                            if (contenidoFila.includes(consulta)) {
                                fila.style.display = 'table-row';
                            } else {
                                fila.style.display = 'none';
                            }
                        });
                    });

                    //PARA ESCRIBIR PURA MAYUSCULA
                    //function convertirAMayusculas(input) {
                    // input.value = input.value.toUpperCase();
                    //}


                </script>
                <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
                    integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
                    crossorigin="anonymous"></script>
            </div>
            <!-- /.container-fluid -->

    </div>
    <!-- End of Main Content -->

    <!-- Footer -->

    <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

    </div>
    <!-- End of Page Wrapper -->

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>

    <!-- Logout Modal-->
    <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-primary" href="login.html">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap core JavaScript-->
    <script src="/src/boostrapdiseñoadmin/vendor/jquery/jquery.min.js"></script>
    <script src="/src/boostrapdiseñoadmin/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

    <!-- Core plugin JavaScript-->
    <script src="/src/boostrapdiseñoadmin/vendor/jquery-easing/jquery.easing.min.js"></script>

    <!-- Custom scripts for all pages-->
    <script src="/src/boostrapdiseñoadmin/js/sb-admin-2.min.js"></script>

</body>

</html>